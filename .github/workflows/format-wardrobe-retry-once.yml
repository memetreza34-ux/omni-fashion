name: Format Wardrobe Retry Once

on:
  pull_request:

concurrency:
  group: format-wardrobe-retry-${{ github.event.pull_request.number }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  format:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout PR branch
        uses: actions/checkout@v6
        with:
          ref: docs/app-development-a-z

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '22.13.x'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Format wardrobe retry UI
        run: npx prettier src/app/index.tsx --write

      - name: Verify formatted UI
        run: npx prettier src/app/index.tsx --check

      - name: Commit formatting only
        run: |
          if git diff --quiet -- src/app/index.tsx; then
            echo "Wardrobe retry UI is already formatted."
          else
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add src/app/index.tsx
            git commit -m "style: format wardrobe retry UI"
            git push origin HEAD:docs/app-development-a-z
          fi

      - name: Verify root quality
        run: npm run lint && npm run format:check && npm run verify:quality
