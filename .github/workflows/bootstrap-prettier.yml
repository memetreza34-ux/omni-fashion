name: Bootstrap Prettier

on:
  pull_request:

permissions:
  contents: read

jobs:
  bootstrap-prettier:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout PR branch
        uses: actions/checkout@v6
        with:
          ref: docs/app-development-a-z

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '22.13.x'
          cache: npm

      - name: Install current dependencies
        run: npm ci

      - name: Install pinned formatter dependencies
        run: npm install --save-dev --save-exact prettier@3.9.6 eslint-config-prettier@10.1.5

      - name: Verify Expo dependency compatibility
        run: npx expo install --check

      - name: Commit dependency files only
        run: |
          if git diff --quiet -- package.json package-lock.json; then
            echo "Formatter dependencies are already installed."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add package.json package-lock.json
          git commit -m "dev-deps: add pinned Prettier toolchain"
          git push origin HEAD:docs/app-development-a-z
